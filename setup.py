from setuptools import setup, find_packages

setup(
    name="jetcamera",
    version="1.0.0",
    description="Easy to use camera on Jetson nano including CSI & USB Camera.",
    packages=find_packages()
)